# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
*.egg-info/

# Environment — never commit secrets
.env

# Model artifacts: *.pkl files are EXCLUDED globally except those in /models
# (the trained model artifacts in /backend/models/ are committed so Render can serve them)
*.pkl
!backend/models/*.pkl
!backend/models/*.json

# Node / React
node_modules/
dist/
build/
.eslintcache

# IDE
.vscode/
.idea/

# System
.DS_Store
Thumbs.db
